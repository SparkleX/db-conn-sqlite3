{
	"name": "db-conn-sqlite3",
	"version": "1.0.1",
	"description": "JDBC like database driver for sqlite3",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"prebuild": "npm run clean",
		"build": "tsc",
		"clean": "rimraf dist",
		"test": "jest",
		"upgrade": "npm i -g npm-check-updates && ncu -u && npm install"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"db-conn": "^1.3.2",
		"sqlite3": "^5.1.6"
	},
	"devDependencies": {
		"@types/jest": "^29.5.1",
		"@types/node": "^20.1.4",
		"@types/sqlite3": "^3.1.8",
		"jest": "^29.5.0",
		"rimraf": "^5.0.0",
		"ts-jest": "^29.1.0",
		"typescript": "^5.0.4"
	}
}